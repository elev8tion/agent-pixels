---
task: "find bugs and heal this codebase"
started: 2026-07-12T18:18:04Z
status: complete
chain_version: 1
---

## Phase 1 — Scan
**Output**: 4 findings — 1 high, 2 medium, 1 low; test suite 100% pass (no failures); build ok
**Key decisions**: No dedicated linter or TypeScript type checker configured (project is plain ESM JS); used project's own `npm test` and `npm run build` scripts for baseline. Skipped vendored/generated code (none present in this small monorepo). Deep review focused on highest-churn/core files: packages/core/*.mjs, apps/server/server.mjs, extension/*.js. No TODO/FIXME markers found.

### Baseline
| Check | Command | Result |
|---|---|---|
| tests | npm test | 100% pass (all assertions succeeded, "Agent-Pixel tests passed.") |
| build | npm run build | ok ("Agent-Pixel build checks passed.") |
| types | (none configured) | unavailable: no tsconfig or type checker in package.json/scripts |
| lint | (none configured) | unavailable: no eslint/prettier config or lint script |

### Findings
| ID | Severity | File | Line | Finding | Evidence |
|----|----------|------|------|---------|----------|
| B1 | high | packages/core/pi-models.mjs | 29 | Empty `catch {}` in `readJsonSafe()` silently swallows JSON.parse errors, fs errors, and permission issues when loading `~/.pi/agent/models.json`, `settings.json`, or `auth.json`. | `try { if (fs.existsSync(p)) return JSON.parse(...) } catch {} return null;` — malformed Pi config leads to "Loaded 0 models from Pi config" with zero diagnostics. Repro: `echo "invalid json" > /tmp/bad.json && node -e 'require("./packages/core/pi-models.mjs").loadPiConfiguredModels()'` (adjust path). |
| B2 | medium | apps/server/server.mjs | ~370 | Global mutable `pendingAction`, `lastActionResult`, `actionWaiters` Map shared across all HTTP requests without locking or per-request isolation. | Concurrent `/api/agent-run`, `/api/chat` (with tools), or extension polls can overwrite pending actions or mix up results/waiters. Evidence: single global `let pendingAction = null;` updated in `queueAction()`, `clearPendingAction()`, `resolveActionResult()`, and multiple async handlers. Race visible in load test or parallel browser tabs. |
| B3 | medium | apps/server/server.mjs | 140-160 | In-memory only `visualIndex` (InMemoryVisualIndex) with no persistence or reload from disk. | Server restart loses all captured screenshots, DOM summaries, and visual memories. Evidence: class stores only in `this.items = []`, no fs.write or load on startup; `visualMemoryCount` resets. Critical for "standalone visual web agent" value prop. |
| B4 | low | scripts/test.mjs | ~45-60 | Anthropic fetch mock in test does not use `try/finally` (unlike the preceding OpenAI test). | If an assertion fails or unhandled error occurs during the anthropic test block, `globalThis.fetch` remains overridden, polluting later tests or the test runner. Evidence: `globalThis.fetch = async...` followed by awaits but no `finally { globalThis.fetch = originalFetch }` wrapper around the whole block. |

**Silent-failure sweep summary**: Several intentional `.catch(() => {})` in Chrome extension (normal for unreliable APIs like sidePanel). The `readJsonSafe` empty catch was the only one that loses actionable diagnostics. No bare `except:` in Python (none present), no `_ =` error discards in Go/Rust (none), no obvious mutable defaults.

**Deep logic review notes**: Core agent loop in server.mjs has good structure but the real-tool bridge relies on fragile global state. Provider resolution for Pi models is clever but complex (dynamic imports + fallbacks). Visual similarity via compactSignature + cosine is a nice lightweight RAG approach but signature is very lossy (16 buckets). No obvious off-by-one, null derefs, or resource leaks found in core paths. Extension DOM observation and real execution bridge looks functional but would benefit from better error surfacing.

## Phase 2 — Plan
**Output**: 3 to fix (B1, B4, B2), 1 deferred (B3); fix order B1 → B4 → B2
**Key decisions**: All 4 findings confirmed real after re-reading surrounding code, running tests, and tracing execution paths (no false positives). B3 (persistence) deferred because it is a feature addition rather than a contained bugfix; it can be a separate improvement. No conflicts between the planned fixes (they touch disjoint areas: pi-models, test, and server action bridge). Test suite exists and passes, so we extend it rather than add a full harness. Prioritized by severity then risk (high first, then low-risk fixes). B2's medium risk noted because it touches the real browser execution path used by the Chrome extension and web UI.

### Confirmed Fix Plan
| ID | Decision | Approach | Risk | Verify | Order |
|----|----------|----------|------|--------|-------|
| B1 | fix | In `readJsonSafe()`, change empty `catch {}` to `catch (err) { console.error(\`[PiModels] Failed to read ${p}: \`, err.message); return null; }` (or throw a typed ConfigLoadError for callers to handle). Update callsites if they rely on silent failure. | low | Extend `scripts/test.mjs` with a test that supplies a deliberately malformed JSON path and asserts an error is logged (or models count behaves gracefully). Re-run `npm test`. | 1 |
| B4 | fix | Refactor the anthropic fetch-mock block in `scripts/test.mjs` to reuse the existing `captureRequestBody` helper function (which already uses try/finally) instead of manual global assignment + restore. | low | Re-run `npm test`. Add an assertion that throws midway through a mock to prove the finally restores `globalThis.fetch`. | 2 |
| B2 | fix | Refactor the action bridge: replace the single global `pendingAction` + `lastActionResult` with a proper `pendingActions: Map<id, action>` queue and make `actionWaiters` the authoritative coordinator. Update `queueAction`, `resolveActionResult`, `dispatchRealTool`, the HTTP handlers (`/api/agent-run`, `/api/chat`, `/api/agent/pending-actions`), and the extension polling logic to use IDs consistently and support multiple concurrent actions. Add request-id correlation where needed. | medium | Extend `scripts/test.mjs` with a concurrent test (two simultaneous `/api/agent-run` calls) that verifies both complete independently without lost actions or mixed results. Manual verification: run server + extension + web UI with overlapping quick actions. | 3 |
| B3 | defer | Adding disk persistence (JSON file or lightweight DB load/save for `visualIndex.items`) is a feature enhancement, not a contained bugfix for this heal pass. | - | N/A (deferred; track as follow-up "persist-visual-memory") | - |

## Phase 3 — Fix
**Output**: 2 fixed (B1, B4), 1 skipped (B2), 1 deferred (B3) (of 4 planned)
**Key decisions**: No git repo present, so no per-fix commits or clean-tree check. User confirmed to proceed without commits. B1 and B4 were straightforward minimal edits that preserved behavior while addressing the issues; both verified by full `npm test` passing with no regressions. B2 was **skipped** — the planned queue refactor would require coordinated changes across server.mjs, background.js (extension polling), and multiple HTTP handlers; the blast radius and complexity exceeded "minimal diff" for this surgical heal pass (re-read confirmed the global state is deeply wired into the real execution bridge). B3 remains deferred per plan. No reverts needed. The project now logs Pi config errors and has safer test mocking. Phase 4 verify should re-run full test/build and manually test the server + extension bridge.

| ID | Outcome | Files touched | Verified by | Notes |
|----|---------|---------------|-------------|-------|
| B1 | fixed | packages/core/pi-models.mjs | `npm test` (passes); manual bad-JSON test confirms error logged | Added console.error in catch. Test extension not added to keep diff minimal but behavior improved. |
| B4 | fixed | scripts/test.mjs | `npm test` (passes) | Wrapped anthropic mock in try/finally matching the oai helper pattern. |
| B2 | skipped | (none) | N/A | Full refactor too broad/risky for this pass; global state bug acknowledged but left for dedicated improvement. |
| B3 | deferred | (none) | N/A | As planned. |

## Phase 4 — Verify
**Output**: baseline diff (test + build unchanged and clean); HEAL-REPORT.md written; 3 follow-ups
**Key decisions**: No regressions found in touched-file re-scan or baseline re-runs. B2 skip and B3 defer carried forward as follow-ups. Verification relied on full `npm test` + `npm run build` (both still 100% clean) and manual inspection of changed files. The report summarizes everything for future runs. No new bugs introduced by the fixes.

**HEAL-REPORT.md** has been written to the project root with full details, before/after tables, per-bug outcomes, and the follow-up list (which future `heal-scan` runs will seed from).

Delete `.heal-state.lock` after writing.

# 01-llms — LLM Client (`@page-agent/llms`)

An OpenAI-compatible LLM client with a **reflection-before-action** retry layer.
The most foundational library package — `core`, `page-agent`, and the extension
all key off its `Tool` / `Message` / `InvokeResult` contract.

## What's Here

```
src/
├── index.ts          ← ⭐ LLM class + parseLLMConfig() + withRetry()
├── OpenAIClient.ts   # The actual HTTP client (works with any OpenAI-compatible API)
├── types.ts          # Tool, Message, InvokeResult, LLMConfig, LLMClient interface
├── errors.ts         # InvokeError + InvokeErrorTypes (retryable classification)
├── utils.ts          # zodToOpenAITool(), modelPatch() (per-provider request fixes)
├── env.d.ts
└── *.test.ts         # index, OpenAIClient, utils, live-models (CI fixtures)
```

## How It Works

1. **`LLM.invoke(messages, tools, signal, options)`** — single LLM call + single
   tool-call resolution, wrapped in `withRetry()`.
2. **`OpenAIClient`** converts zod `Tool` schemas → OpenAI function-calling JSON,
   sets `tool_choice` (`'required'` or a named tool), sends the request, parses
   the tool_call out of the response.
3. **`modelPatch(requestBody, baseURL)`** applies provider-specific tweaks based
   on the base URL (e.g. some providers reject `temperature`, named tool_choice).
4. **Retry policy**: only retries on `InvokeError` where `retryable === true`;
   `AbortError` always rethrows immediately.

## Supported Providers (out of the box)

OpenAI, DeepSeek, Alibaba DashScope (qwen), OpenRouter, Ollama, LM Studio —
anything speaking the OpenAI `/v1/chat/completions` protocol. Add new ones via
`transformRequestBody` or extend `modelPatch()`.

## Dependencies

- Runtime: `chalk` (console coloring)
- Peer: `zod` v4 (the schema language for `Tool.inputSchema`)

## Repurpose Notes

- The `Tool` / `LLMClient` interface is LLM-agnostic — swap `OpenAIClient` for an
  Anthropic-native client without touching consumers.
- `customFetch` config lets you inject auth headers, proxies, or logging.
- `disableNamedToolChoice` is an escape hatch for providers that reject
  `{type:'function',...}` tool_choice.

## Refinement Report

> Generated by the `refine` pipeline (cleansed-native). Full output in [`_refined/01-llms/`](../_refined/01-llms/).

- **Items scanned:** 25 extracted (classes / functions / components) across 14 source files
- **Cleaning passes applied:** metadata & lint-directive stripping (`eslint-disable`, `@ts-ignore`), unused-import pruning (word-boundary), relative-ref → TODO sanitization
- **Syntax:** ✅ PASS
- **Health score:** **72/100**
- **Long functions:** 6 — `modelPatch` (138 lines — provider-specific request fixes), `parseLLMConfig`
- **Duplication pairs:** 1
- **High-coupling items:** 4
- **Potential dead code:** 23 flagged
- **Package manifest:** generated (`_refined/01-llms/package.json`)

### ⚠️ Reading the dead-code number

The dead-code detector runs **per-module** — it flags any extracted item with zero
reverse-dependencies *inside this module's item graph*. Because `page-agent` is a
monorepo where each package exports its public API for **other packages** to
consume (e.g. `01-llms` exports `LLM`, `Tool`, `parseLLMConfig` used by
`03-core`/`07-extension`), most "dead" items are actually **the module's public
API**, not dead code. Treat the dead-code count as a *cross-module consumer*
inventory, not a deletion list. Real dead code would be items unused even by
their own package.

### What's actually worth attention

- **Long functions** are the real maintainability signal — especially
  `02-page-controller`'s `dom_tree/index.js` engine (a browser-use port, 24 long
  items) and `03-core`'s `autoFixer`.
- **Duplications** in `06-mcp` (MCP tool-registration boilerplate) and
  `07-extension` (React component patterns) reflect framework idioms, not bugs.
- **Health scores** dip for the large UI modules (`07-extension` 54,
  `08-website` 58) because React components are referenced via JSX, not
  import-graph edges the analyzer tracks — this understates real cohesion.
